apiVersion: v1
kind: # https://kubernetes.io/docs/concepts/configuration/configmap/
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: dev
data:
  ENVIRONMENT: "dev"
  VERSION: "1.0.0"
  FEATURE_FLAG_NEW_UI: "true"

